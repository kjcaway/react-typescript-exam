### React + Typescript Example
react typescript practice...
- Counter Example

### Tip
- Prettier 적용법
    ```
    1. ctrl + p 입력 후, format document 설정 선택
    2. Formatter 기본값을 Prettier로 설정(파일마다 설정해줘야하는듯)
    ```
- react-redux는 타입스크립트 기본 지원x. 아래와 같이 따로 설치
    ```bash
    yarn add @types/react-redux
    ```